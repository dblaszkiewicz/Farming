export interface PlantWarehouse {
  id: string;
  name: string;
}

export interface FertilizerWarehouse {
  id: string;
  name: string;
}

export interface PesticideWarehouse {
  id: string;
  name: string;
}
